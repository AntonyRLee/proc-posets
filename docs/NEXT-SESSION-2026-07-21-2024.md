# Next-session handoff — procposets — 2026-07-21 (20:24)

**Branch:** `output-sensitive-extract-signature` (**pushed**; in sync with `origin/…`, base is `main`) ·
**Build/tests:** last-known **358 passed, 13 skipped** (`uv run pytest -q`, green after every commit) ·
**Consumers:** PMN **201**, SPM/experiments **54** (both unchanged) ·
**Uncommitted:** working tree clean (only untracked `NEXT-SESSION-2026-07-20.md`, meant to stay untracked).

## Where we are (1–2 sentences)
**Phase 4 (API evolution) is fully landed** behind deprecation aliases and pushed to remote — all 9
api-breaking-rename items done, both cut-over consumers still green. The whole 8-phase byte-exact refactor
is now landed **except Phase 7** (efficiency stress test) and the Phase-6 `viz/string_diagram.py` file-split.
Plan + live log: **`docs/2026-07-20-refactoring-plan.md`**.

## Done this session (Phase 4 — 8 refactor commits + 1 docs, `6ecd8a0`..`b351ed5`)
- `6ecd8a0` **Items 5/1/3** — `inL`→`in_L` param rename; e(P) and series/parallel **docstring notes only**
  (functional merge impossible — 3 different input types; root `series`/`parallel` already the SPTree ctors).
- `3c03c17` **Item 6** — dead `CompositeDiagram.canonical_key` → `label_multiset_key` (resolves the 3-way
  `canonical_key`/`canon_key` ambiguity; the two live key fns unchanged).
- `8ceb3bf` **Item 8** — `report_vs_trivial` → `trivial_report` + **mandatory** module alias; surfaced in
  `_LAZY`/`__all__`. Break only shows in the PMN demo golden.
- `b60e305` **Item 2** — `sample_extension` → `sample_extension_tree` (SPTree) / `sample_extension_poset`
  (DP engine); `sample_extension` alias kept wired to the TREE sampler (PMN imports+calls it).
- `4272542` **Item 7** — public `color_map`→`colour_map`+alias; new **`viz/palette.py`** single-sources every
  hex (3 list palettes kept SEPARATE). Byte-exact dag `render_dag` DOT vs live cpm.
- `de64d37` **Item 4** — `Atom.noise`→`noise_kernel` + deprecating `.noise` **property** + `make_atom(noise=)`
  deprecated kwarg (TypeError if both). `describe()` keeps the byte-pinned `noise=` token.
- `842bbd2` **Item 9** — the 14 viz style globals → **`StringDiagramStyle` = LayoutStyle(4)+DrawStyle(10)**
  frozen dataclasses, threaded through the term-DSL + layout/draw fns; `render(style=None)` bridges from the
  still-present globals (legacy override path byte-identical). **This unblocks the Phase-6 viz file-split.**
- `b351ed5` **Docs** — Phase 4 execution log.
- **Then pushed:** 40 commits `1ff8808..b351ed5` to `origin/output-sensitive-extract-signature`.

Every non-`none` item gated with a fixed-`PYTHONHASHSEED=0` before/after capture (byte-identical) + full
suite + both consumer suites. Driven by a **10-agent read-only discovery workflow** (consumer-breakage matrix).

## Open decisions (need the user)
- **Which next?** User HELD Phase 7 ("no point making it more efficient if something isn't working"). Two
  candidates below — pick one, or say when to unhold Phase 7.
- **Merge to `main`?** Branch is a pushed feature branch, not merged. Say the word for a PR/merge.

## Next steps — ranked
1. **Phase-6 `viz/string_diagram.py` file-split** — now UNBLOCKED by Item 9. Split into `viz/_layout.py`
   (term DSL + Layout/PlacedBox/Wire + `_box_sub`/`_seq`/`_par`/`_finish`, uses only `LayoutStyle`, stays
   matplotlib-free) + the matplotlib drawing side. Geometry constants (`_BW`.._TRIM`, `_LANE_*`, `_RISER`)
   read by BOTH halves → put in `_layout.py` (or a shared `_geom.py`) and import into drawing. Palette
   already in `viz/palette.py`. **Figures aren't byte-pinned → the new override test + a layout-coords
   before/after capture are the safety net.**
2. **Phase 7 — efficiency stress map** (the stated end goal, currently HELD). §7 of the plan = 13-row hotspot
   table. Serial, cgroup-capped; probe AT the load-bearing caps; every speedup golden-locked against the slow twin.
3. **Phase 4 alias REMOVAL** (per-consumer cut-over): migrate PMN/SPM off the old names, then drop the aliases
   (`sample_extension`, `report_vs_trivial`, `color_map`, `Atom.noise` property + `make_atom(noise=)`).
4. **Port `sim/cpm`** (`arl/sosym`, `string-diagram-process-mining/sim`) — the 3rd, un-ported consumer.
5. Tree-block flatten skeleton (`matrix._block_sequence`/`discrete._block_items`) — deferred; windowing part
   is `changes-values`, so not a clean value-preserving dedup.

## Gotchas / don't-break
- **HARD RESOURCE RULES (project CLAUDE.md):** every local run under
  `systemd-run --user --scope -q -p MemoryMax=3G -p MemorySwapMax=0 timeout <s> …`; one heavy process at a
  time; NO parallel local compute (read-only text agents OK). Use plain **`uv`, not `rtk`**, for byte-exact
  stdout — `rtk` swallows it (prints "ok"); route through a file or use `2>file` for real output.
- **Phase 4 aliases are load-bearing — DON'T remove yet:** `sample_extension = sample_extension_tree`,
  `report_vs_trivial = trivial_report`, `color_map = colour_map`, `Atom.noise` property, `make_atom(noise=)`.
  PMN imports/reads several. Removal is the per-consumer cut-over task (#3), not done here.
- **Item 9 bridge is deliberate:** the 14 style globals + `render(style=None)→_style_from_globals()` STAY until
  an off-disk manuscript/tikz repo (`string-diagram-process-mining/{sections,tikz}`) is grepped for overrides.
  Deleting the globals now is an unverified api-break. `describe()` MUST keep printing `noise=` (not
  `noise_kernel=`) — byte-pinned in the PMN demo golden.
- **Two items were docs-only by necessity (not laziness):** e(P) (item 1) and series/parallel (item 3) can't
  be functionally merged — 3 different input types; the root `series`/`parallel` already bind the SPTree ctors.
- **Locked decisions (don't revert):** two renderers stay separate (SPTree `->`/`||` vs moddecomp `;`/`*`);
  `Poset`(id+label) vs `Rel`(distinct-label) split; byte-exact-on-VALUES (rename/move freely, never change a
  number/string). `extract_classes` left un-split. viz palettes stay 3 SEPARATE lists (merging shifts colours).
- **Golden skips = 13**, all vestigial estimation/poset originals deleted from the two cut-over repos. The
  `[graph]`/`[viz]`/`[pm4py]` goldens RUN and cross-check the live `sim/cpm` (path fixed in Phase 6).

## Pointers
- **Plan + live execution log:** `docs/2026-07-20-refactoring-plan.md` (read FIRST — §7 stress-test table,
  per-phase status incl. the new "Phase 4" and "Phase 2 leftovers" entries).
- **Discovery artifacts (this session):** the Phase-4 10-agent map is in the workflow transcript under
  `…/subagents/workflows/wf_328796df-1ee/` (journal.jsonl) — has the full consumer-breakage matrix.
- Memory slug: `refactoring-plan-progress` (updated with Phase 4 + remaining-work).
- Test procposets: `systemd-run --user --scope -q -p MemoryMax=3G -p MemorySwapMax=0 timeout 480 uv run pytest -q`.
- Test consumers: `cd ~/Research/poset-mixture-npmle && uv run pytest -q` ;
  `cd ~/Research/stochastic_process_mining/experiments && uv run pytest -q --ignore=tests/test_pm4py_adapters.py --ignore=tests/test_signature_lift.py`.
- Prior handoffs: `NEXT-SESSION-2026-07-21-1857.md` (Phases 5+6), `NEXT-SESSION-2026-07-21.md` (Phases 0–3).
