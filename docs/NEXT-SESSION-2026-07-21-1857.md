# Next-session handoff — procposets — 2026-07-21 (18:57)

**Branch:** `output-sensitive-extract-signature` (NOT pushed; base is `main`) ·
**Build/tests:** last-known **356 passed, 13 skipped** (`uv run pytest -q`, green after every commit this session) ·
**Uncommitted:** working tree clean. ⚠️ `NEXT-SESSION-2026-07-20.md` (prior session's handoff, was meant to stay untracked) got **accidentally committed** in `10b028e` via a `git add -A` — harmless, but see Gotchas for the one-line un-track if you want it back out.

## Where we are (1–2 sentences)
Phases **0–6** of the 8-phase refactoring plan are landed (Phase 6 complete **except** the
`viz/string_diagram.py` split, deferred to Phase 4). The big discovery: **2 of the 3 consumer
repos are already cut over** to an editable `../procposets` dep and stay green — so procposets
changes hit them immediately. Plan + live log: **`docs/2026-07-20-refactoring-plan.md`**.

## Done this session (6 commits, `46a49f4`..`1e2d1c1`)
- `46a49f4` **Phase 5** — layer-guard tests for `[graph]`/`[viz]`/`[pm4py]` extras (both-directions,
  `importorskip` positives, non-vacuity mutation-checked). Suite 349/20.
- `c440f4c` **Golden path fix** — the sim repo was renamed `-DIAGRAM-…-v2/sim` →
  `string-diagram-process-mining/sim`; three goldens hardcoded the old path and were silently
  skipping. Repointed → **+7 byte-exact cross-checks now green** (procposets == live `cpm`).
- `10b028e` **Phase 6 moves** — `equivalence.py`→`cospan/equivalence.py`+shim; `_log_mean_exp_rows`
  →`_numerics.py` (shared by oracle+npmle); `from_petri` re-export from `adapters/__init__`;
  cospan `__init__` docstring. *(also accidentally committed the 07-20 handoff — see Gotchas.)*
- `f148d02` **Phase 6 split** — `rel.py` byte-exact line-slice → `rel.py` (relation algebra + ideal-DP)
  + `rel_sp.py` (SP-tree view) + `rel_classes.py` (hypothesis classes); re-export at base keeps
  `procposets.rel` + the `poset_mixture.posets` shim unchanged. Dropped a dead `math.comb`.
- `1e2d1c1` **Docs** — Phase 6 execution log + consumer-repo reality correction.

**Consumer stability (end-gate, this session):** poset-mixture-npmle **201 passed** · spm/experiments
**54 passed** — both unchanged vs baseline, against post-Phase-6 procposets.

## Open decisions (need the user)
- **Which phase next?** Options ranked below. Stated end goal (session 1) is **Phase 7**.
- **The accidentally-committed `NEXT-SESSION-2026-07-20.md`** — leave it, or un-track it (Gotchas).

## Next steps — ranked
1. **Phase 7 — efficiency stress test** (the stated end goal). §7 of the plan has a 13-row hotspot
   map. Serial, cgroup-capped harness; probe *at* the load-bearing caps; every speedup golden-locked.
2. **Phase 4 — api-breaking renames** (unblocks the deferred `string_diagram` split via the
   style-globals→config-dataclass refactor; also the e(P)/`then`-`par`/`noise_kernel`/`canonical_key`
   unifications). Do behind deprecation aliases; coordinate with `sim/cpm` (the un-ported repo).
3. **Finish the now-unblocked Phase-2 `[graph]`/`[pm4py]` dedups** — `occn/_lift.py`,
   `cospan/_boundary.py`, adapter build/validate wrappers. Previously "can't byte-verify standalone";
   **now byte-verifiable** against the on-disk `sim/cpm` via the fixed golden path.
4. **Port `sim/cpm`** (`arl/sosym`) over to procposets — the 3rd consumer, still on its own copy.
   Larger; the `[graph]`/`[pm4py]` surface.
5. Small Phase 1–3 leftovers (value-neutral): bridge `p→P`, single-source the `Bundle` alias
   (`engine.py:31`+`typebalance.py:32`), ship `py.typed`.
6. Fix spm's pre-existing broken test collection (missing `demo/11_synthetic_fleet/signature_lift.py`)
   — **in the spm repo**, not procposets.

## Gotchas / don't-break
- **Consumer reality (corrects the earlier "not checked out" framing):** all 3 repos are on disk under
  `~/Research/`. **poset-mixture-npmle** (`main`) and **stochastic_process_mining/experiments**
  (`arl/main`) are **already ported** (editable `../procposets` dep, `__init__` re-export shims,
  originals deleted) → any procposets change affects them; gate = run their suites. **sim** at
  `string-diagram-process-mining/sim` (`arl/sosym`) is **NOT** ported (own `cpm/` copy).
- **Golden skips:** now **13** (was 20). The 13 are estimation/poset goldens whose originals were
  **deleted** from the two cut-over repos — vestigial; their "not checked out" messages are misleading.
  The `[graph]`/`[viz]` goldens now RUN (path fixed) and cross-check the live `cpm`.
- **`string_diagram` split is BLOCKED on Phase 4** — its layout fns read demo-overridable mutable style
  globals (`STRAIGHT_SPINE`/`TYPE_LANES`/`PORT_ORDER_KEY`/`ALIGN_BOUNDARY_STUBS`, 12 sites) + share
  `_BW`/`_BOX_PAD` with drawing. Do the globals→config-dataclass refactor first. Don't split naively:
  the viz goldens render with defaults, so a broken override API would pass silently.
- **HARD RESOURCE RULES (project CLAUDE.md):** every local run under
  `systemd-run --user --scope -q -p MemoryMax=3G -p MemorySwapMax=0 timeout <s> …`; one heavy process
  at a time; no parallel local compute (text-only agents OK). Use plain **`uv`, not `rtk`**, for
  byte-exact stdout (`rtk` swallows it).
- **Un-track the mis-committed handoff (if wanted):** `git rm --cached NEXT-SESSION-2026-07-20.md &&
  git commit -m "chore: un-track stray prior handoff"` (keeps the file on disk). Branch isn't pushed.
- **Locked decisions (don't revert):** two renderers stay separate; `Poset` (id+label) vs `Rel`
  (distinct-label) split; fleet-wide `|X|` in `smd_rows`; byte-exact-on-VALUES (rename/move freely,
  never change a number). `extract_classes` left un-split (delicate closures — net-negative).

## Pointers
- **Plan + live execution log:** `docs/2026-07-20-refactoring-plan.md` (read FIRST — §7 stress-test
  hotspot table, per-phase status, deferrals).
- Memory slug: `refactoring-plan-progress` (updated with the consumer-repo reality + Phase 6).
- Test procposets: `systemd-run --user --scope -q -p MemoryMax=3G -p MemorySwapMax=0 timeout 480 uv run pytest -q` (from repo root).
- Test consumers: `cd ~/Research/poset-mixture-npmle && uv run pytest -q` ; `cd ~/Research/stochastic_process_mining/experiments && uv run pytest -q --ignore=tests/test_pm4py_adapters.py --ignore=tests/test_signature_lift.py`.
- Prior handoff: `NEXT-SESSION-2026-07-21.md` (Phases 0–3).
